# Sprint 12 — Filters and URL state

**Prerequisites:** 11. README defines execution order; IDs preserve the original package.
**Read first:** docs/00-overview.md, metrics-and-evidence.md, data-model.md and relevant seed/copy/design references.

## Build

useFilter/useComparison/useFilterHref from contract. Modal draft Apply atomic, Cancel discard, Clear reset. Multi-select eligible counts; removable value/dimension chips; cmp separate.

URL keys plus v=2 and local w. Back restores applied states; typing uses replace, Apply push. Topic detail syncs route topic visibly. Unknown IDs/dataset version/local groups show recovery. Basic saved views now: name/filter/cmp/mode/version/workspace. Normalized matching controls star; preserve cmp order. No URL promises to transfer local overlays.

## Acceptance

Apply/Cancel/back; cross-page preservation; URL round trip; missing group warns; URL cannot widen source boundary.

Run build/lint/typecheck; seed:check from 03 onward. Numerical changes run contract tests; interaction changes run relevant browser smoke. Record results/deviations in docs/BUILD-LOG.md. Never mark an unrun check passed.

Frontend demo only. Implement assigned sprint, preserve earlier behavior, do not start next sprint automatically.

**Suggested commit:** sprint-12: filters and url state
